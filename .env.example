# Copy to .env and fill in. The real .env is gitignored.
# Google Maps JavaScript API key (Places library) for quote-form address autocomplete.
# Leave blank to disable autocomplete — the address fields stay as plain text inputs.
VITE_GOOGLE_MAPS_API_KEY=
